streamlit>=1.32.0
plotly>=5.19.0
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0

# Drift on-chain data (api/drift.py)
driftpy>=0.8.80
anchorpy>=0.20.0
solana>=0.34.0
solders>=0.21.0
nest_asyncio>=1.6.0   # needed when Streamlit runs its own async loop
